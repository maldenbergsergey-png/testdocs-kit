# Правила подготовки тестирования задачи

**Статус:** организационный baseline, требующий человеческого review.

Task-first workflow выбирается по пользовательскому intent, а не по знанию имён skills.

## Ветви workflow

- **CHECKLIST_ONLY:** запрос явно просит чек-лист. Соберите релевантный контекст и сформируйте Jira Wiki checklist. Не запускайте полный TMS-анализ; читайте отдельные cases только когда они нужны для понимания требования или очевидного regression impact.
- **FULL_PACKAGE:** обычное «подготовь тестирование» и запрос полного набора. Соберите контекст, создайте task checklist, выполните targeted discovery постоянного покрытия, классифицируйте scenarios и подготовьте полные CREATE/UPDATE proposals.

Task checklist содержит всё, что важно проверить в текущем изменении, включая one-time rollout/migration/temporary checks. Permanent Test Case создаётся только для стабильного, повторяемого, наблюдаемого и диагностически самостоятельного сценария с долгосрочной regression value.

Для каждого сценария внутренне определите: нужен ли он для задачи, имеет ли постоянную ценность, найдено ли существующее покрытие и соответствует ли оно новому поведению. Для постоянного покрытия используйте `CREATE`, `UPDATE`, `NO_CHANGE`, `RETIRE_PROPOSAL`, `INSUFFICIENT_CONTEXT`; пользователю показывайте «Создать», «Актуализировать», «Без изменений», «Сделать неактуальным», «Требует уточнения».

## Полный пользовательский результат

По умолчанию отвечайте по-русски и показывайте:

1. `Кратко о задаче` — 1–3 предложения и только реально прочитанные источники.
2. `Чек-лист тестирования` — готовый Jira Wiki Markup в fenced `text` block.
3. `Создать Test Cases — N` — каждый case целиком по `test-case-standard.md`, либо явное отсутствие необходимости.
4. `Актуализировать Test Cases — N` — причина, focused diff `ДОБАВИТЬ/ИЗМЕНИТЬ/УДАЛИТЬ/ОСТАВИТЬ` и полная предлагаемая версия.
5. `Сделать неактуальными — N` — key/URL/name, исчезнувшее или заменённое поведение, причина и предложение сменить организационный статус после review; никогда не удалять.
6. `Без изменений` — все реально проанализированные релевантные cases и причина достаточности.
7. `Ограничения и вопросы` — conflicts, partial/unavailable discovery и недостающие правила.

Если ни одно изменение постоянного покрытия не требуется, явно скажите это для создания, актуализации и retire и перечислите достаточные cases. Не показывайте пользователю названия внутренних skills, raw bundle или успешно восстановленные ошибки интеграций.

При частичном контексте выдайте безопасную часть и вынесите неопределённое в вопросы. Частичный TMS search не доказывает отсутствие coverage и не позволяет категоричный `CREATE`.
