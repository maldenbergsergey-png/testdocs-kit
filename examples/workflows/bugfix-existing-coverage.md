# Bugfix уже покрыт

`DEMO-T50` проверяет правильное ожидаемое поведение и упал бы на исправляемом дефекте. Решение: `NO_CHANGE` / «Без изменений». Новый case не создаётся только из-за наличия bug issue.
