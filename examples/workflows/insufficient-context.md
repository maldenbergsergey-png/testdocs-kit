# Частичный контекст

Безопасно определённые проверки включены в checklist. Неописанное правило доступа вынесено в «Требует уточнения». TMS discovery имеет статус `PARTIAL`, поэтому решение о создании постоянного case — `INSUFFICIENT_CONTEXT`, а не категоричный `CREATE`.
