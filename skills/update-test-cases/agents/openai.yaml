interface:
  display_name: "Update Test Cases"
  short_description: "Propose read-only changes to existing test cases"
  default_prompt: "Use $update-test-cases to propose a reviewable update to the existing test case without changing it in the TMS."
