interface:
  display_name: "Update Test Cases"
  short_description: "Propose updates or correct a just-created case"
  default_prompt: "Use $update-test-cases to propose a reviewable case update. Apply it only when I explicitly ask and the MCP confirms the case was created in this running session."
